from distutils.core import setup

setup(
    name="django_utils",
    version='0.0.1',
    description="Common utils, shortcuts and template tags for the Django framework",
    author="Anton Konyshev",
    packages=["django_utils"],
)
